---
title: "Untitled"
output: html_document
date: "2024-03-26"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
# Instalación automática de paquete.

#Este bloque se encarga de instalar los paquetes necesarios.



packages = c("tidyverse","knitr","reshape2")
package.check <- lapply(packages, FUN = function(x) {
  if (!require(x, character.only = TRUE)) {
    install.packages(x, dependencies = TRUE,repos='http://cran.rediris.es')
  }
  library(x, character.only = TRUE)
})

search()

```

```{r}
ruta <- "TicketPDF2TXT.ipynb"
system(paste("ipynb", ruta))
l_horas <- list.dirs(ruta, recursive = FALSE)

for (hora in l_horas){
  l_ficheros <- list.files(hora)
  trabajo(hora, l_ficheros)
}

```

